# Archived modules

The **Nav**, **Preview**, and **Edit** modules — superseded by the unified
**Files** module (`tools/files-module/`), which folds browse + image preview +
text edit into one pane with Enter-to-open / Esc-to-back. These three served as
the proving ground for that design; kept here for reference, not installed.
